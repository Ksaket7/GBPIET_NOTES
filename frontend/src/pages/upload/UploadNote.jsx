import UploadForm from "../../components/upload/UploadNote";

const UploadNotes = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center px-4">
      <UploadForm />
    </div>
  );
};

export default UploadNotes;
